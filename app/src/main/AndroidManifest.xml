<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ground0.likeminds" >
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="com.ground0.likeminds.LoginActivity"
            android:screenOrientation="portrait"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"
            android:noHistory="true">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>

        <activity
            android:name="com.ground0.likeminds.LandingActivity"
            android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="com.ground0.likeminds.UnifiedPostActivity"
            android:parentActivityName="com.ground0.likeminds.LandingActivity"
            android:label="Create Post" >

        </activity>
        <activity android:name="com.ground0.likeminds.ProfileViewActivity"
            android:parentActivityName="com.ground0.likeminds.LandingActivity"
            android:label="Group" >

        </activity>

        <activity android:name="com.ground0.likeminds.UserProfile"
            android:parentActivityName="com.ground0.likeminds.LandingActivity"
            android:label="Group" >

        </activity>
    </application>

</manifest>
